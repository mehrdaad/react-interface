import { Flex } from 'grid-styled'
import {
  borders,
  borderRadius,
  borderColor,
  border,
  boxShadow,
} from 'styled-system'

export default Flex.extend`
  ${borders}
  ${borderRadius}
  ${borderColor}
  ${boxShadow}
`
