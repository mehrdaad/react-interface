import { Box } from 'grid-styled'
import {
  borders,
  borderRadius,
  borderColor,
  border,
  boxShadow,
} from 'styled-system'

export default Box.extend`
  ${borders}
  ${borderRadius}
  ${borderColor}
  ${boxShadow}
`
